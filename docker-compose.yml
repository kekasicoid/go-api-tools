version: "3.8"

services:
  app:
    build:
      context: .
    container_name: go-api-tools-app
    ports:
      - "8080:8080"
    environment:
      - HTTP_PORT=8080
      - CORS_ORIGIN=${CORS_ORIGIN}
      - REDIS_ADDR=redis:6379
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - REDIS_DB=${REDIS_DB}
    depends_on:
      - redis

  redis:
    image: redis:latest
    container_name: go-api-tools-redis
    ports:
      - "6379:6379"
    command: [ "redis-server", "--requirepass", "${REDIS_PASSWORD}" ]
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD}
